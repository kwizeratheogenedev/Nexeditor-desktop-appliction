[package]
name = "nexeditor_desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "1.4", features = ["api-all"] }

[package.metadata]
backend_path = "../Nexteditor/backend/server.js"
